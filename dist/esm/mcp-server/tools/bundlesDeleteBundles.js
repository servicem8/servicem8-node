/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { bundlesDeleteBundles } from "../../funcs/bundlesDeleteBundles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult } from "../tools.js";
const args = {
    request: operations.DeleteBundlesRequest$inboundSchema,
};
export const tool$bundlesDeleteBundles = {
    name: "bundles-delete-bundles",
    description: `Delete a Bundle


			
In ServiceM8, records are never deleted, but are archived. Archived records will remain accessible via the API as (active = 0), however will no longer be visible in UI. Archived records can be restored to active by setting the record active field to 1.

			`,
    args,
    tool: async (client, args, ctx) => {
        const [result, apiCall] = await bundlesDeleteBundles(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        const value = result.value;
        return formatResult(value, apiCall);
    },
};
//# sourceMappingURL=bundlesDeleteBundles.js.map