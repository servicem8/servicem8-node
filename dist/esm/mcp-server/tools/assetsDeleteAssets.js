/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */
import { assetsDeleteAssets } from "../../funcs/assetsDeleteAssets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult } from "../tools.js";
const args = {
    request: operations.DeleteAssetsRequest$inboundSchema,
};
export const tool$assetsDeleteAssets = {
    name: "assets-delete-assets",
    description: `Delete an Asset


			
In ServiceM8, records are never deleted, but are archived. Archived records will remain accessible via the API as (active = 0), however will no longer be visible in UI. Archived records can be restored to active by setting the record active field to 1.

			`,
    args,
    tool: async (client, args, ctx) => {
        const [result, apiCall] = await assetsDeleteAssets(client, args.request, { fetchOptions: { signal: ctx.signal } }).$inspect();
        if (!result.ok) {
            return {
                content: [{ type: "text", text: result.error.message }],
                isError: true,
            };
        }
        const value = result.value;
        return formatResult(value, apiCall);
    },
};
//# sourceMappingURL=assetsDeleteAssets.js.map