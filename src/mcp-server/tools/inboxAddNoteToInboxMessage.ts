/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxAddNoteToInboxMessage } from "../../funcs/inboxAddNoteToInboxMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AddNoteToInboxMessageRequest$inboundSchema,
};

export const tool$inboxAddNoteToInboxMessage: ToolDefinition<typeof args> = {
  name: "inbox-add-note-to-inbox-message",
  description: `Add note to message

Adds a note to an inbox message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxAddNoteToInboxMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
