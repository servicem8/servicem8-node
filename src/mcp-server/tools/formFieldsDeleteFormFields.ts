/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { formFieldsDeleteFormFields } from "../../funcs/formFieldsDeleteFormFields.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteFormFieldsRequest$inboundSchema,
};

export const tool$formFieldsDeleteFormFields: ToolDefinition<typeof args> = {
  name: "form-fields-delete-form-fields",
  description: `Delete a Form Field


			
In ServiceM8, deleting a record sets its \`active\` field to \`0\`. Inactive records are still accessible on the API, but are hidden in the UI. Inactive records can be restored by setting their \`active\` field to \`1\`.

			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_forms**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await formFieldsDeleteFormFields(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
