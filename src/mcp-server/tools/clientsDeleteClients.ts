/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientsDeleteClients } from "../../funcs/clientsDeleteClients.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteClientsRequest$inboundSchema,
};

export const tool$clientsDeleteClients: ToolDefinition<typeof args> = {
  name: "clients-delete-clients",
  description: `Delete a Client


			
In ServiceM8, records are never deleted, but are archived. Archived records will remain accessible via the API as (active = 0), however will no longer be visible in UI. Archived records can be restored to active by setting the record active field to 1.

			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_customers**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientsDeleteClients(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
