/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { companyContactsDeleteCompanyContacts } from "../../funcs/companyContactsDeleteCompanyContacts.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteCompanyContactsRequest$inboundSchema,
};

export const tool$companyContactsDeleteCompanyContacts: ToolDefinition<
  typeof args
> = {
  name: "company-contacts-delete-company-contacts",
  description: `Delete a Company Contact


			
In ServiceM8, deleting a record sets its \`active\` field to \`0\`. Inactive records are still accessible on the API, but are hidden in the UI. Inactive records can be restored by setting their \`active\` field to \`1\`.

			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_customer_contacts**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await companyContactsDeleteCompanyContacts(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
