/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { categoriesUpdateCategories } from "../../funcs/categoriesUpdateCategories.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateCategoriesRequest$inboundSchema,
};

export const tool$categoriesUpdateCategories: ToolDefinition<typeof args> = {
  name: "categories-update-categories",
  description: `Update a Category


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_job_categories**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await categoriesUpdateCategories(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
