/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeArticlesDeleteKnowledgeArticles } from "../../funcs/knowledgeArticlesDeleteKnowledgeArticles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteKnowledgeArticlesRequest$inboundSchema,
};

export const tool$knowledgeArticlesDeleteKnowledgeArticles: ToolDefinition<
  typeof args
> = {
  name: "knowledge-articles-delete-knowledge-articles",
  description: `Delete a Knowledge Article


			
In ServiceM8, records are never deleted, but are archived. Archived records will remain accessible via the API as (active = 0), however will no longer be visible in UI. Archived records can be restored to active by setting the record active field to 1.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeArticlesDeleteKnowledgeArticles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
