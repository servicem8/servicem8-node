/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentTemplatesListDocumentTemplates } from "../../funcs/documentTemplatesListDocumentTemplates.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$documentTemplatesListDocumentTemplates: ToolDefinition = {
  name: "document-templates-list-document-templates",
  description: `List all Document Templates


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_templates**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await documentTemplatesListDocumentTemplates(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
