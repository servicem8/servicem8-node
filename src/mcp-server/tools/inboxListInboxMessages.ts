/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxListInboxMessages } from "../../funcs/inboxListInboxMessages.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ListInboxMessagesRequest$inboundSchema,
};

export const tool$inboxListInboxMessages: ToolDefinition<typeof args> = {
  name: "inbox-list-inbox-messages",
  description: `List inbox messages

Retrieves a paginated list of inbox messages with optional filtering`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxListInboxMessages(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
