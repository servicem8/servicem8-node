/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { notesListNotes } from "../../funcs/notesListNotes.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$notesListNotes: ToolDefinition = {
  name: "notes-list-notes",
  description: `List all Notes


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_job_notes**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await notesListNotes(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
