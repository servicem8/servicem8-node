/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { assetsUpdateAssets } from "../../funcs/assetsUpdateAssets.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAssetsRequest$inboundSchema,
};

export const tool$assetsUpdateAssets: ToolDefinition<typeof args> = {
  name: "assets-update-assets",
  description: `Update an Asset`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await assetsUpdateAssets(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
