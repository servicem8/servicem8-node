/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { locationsDeleteLocations } from "../../funcs/locationsDeleteLocations.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteLocationsRequest$inboundSchema,
};

export const tool$locationsDeleteLocations: ToolDefinition<typeof args> = {
  name: "locations-delete-locations",
  description: `Delete a Location


			
In ServiceM8, records are never deleted, but are archived. Archived records will remain accessible via the API as (active = 0), however will no longer be visible in UI. Archived records can be restored to active by setting the record active field to 1.

			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_locations**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await locationsDeleteLocations(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
