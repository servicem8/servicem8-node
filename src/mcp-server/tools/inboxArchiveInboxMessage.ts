/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxArchiveInboxMessage } from "../../funcs/inboxArchiveInboxMessage.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ArchiveInboxMessageRequest$inboundSchema,
};

export const tool$inboxArchiveInboxMessage: ToolDefinition<typeof args> = {
  name: "inbox-archive-inbox-message",
  description: `Archive or unarchive message

Archives or unarchives an inbox message`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxArchiveInboxMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
