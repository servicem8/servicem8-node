/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { knowledgeArticlesGetKnowledgeArticles } from "../../funcs/knowledgeArticlesGetKnowledgeArticles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetKnowledgeArticlesRequest$inboundSchema,
};

export const tool$knowledgeArticlesGetKnowledgeArticles: ToolDefinition<
  typeof args
> = {
  name: "knowledge-articles-get-knowledge-articles",
  description: `Retrieve a Knowledge Article


			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_knowledge**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await knowledgeArticlesGetKnowledgeArticles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
