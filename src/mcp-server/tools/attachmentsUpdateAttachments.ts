/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attachmentsUpdateAttachments } from "../../funcs/attachmentsUpdateAttachments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAttachmentsRequest$inboundSchema,
};

export const tool$attachmentsUpdateAttachments: ToolDefinition<typeof args> = {
  name: "attachments-update-attachments",
  description: `Update an Attachment


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_attachments**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await attachmentsUpdateAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
