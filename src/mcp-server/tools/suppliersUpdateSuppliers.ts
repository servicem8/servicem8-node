/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { suppliersUpdateSuppliers } from "../../funcs/suppliersUpdateSuppliers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateSuppliersRequest$inboundSchema,
};

export const tool$suppliersUpdateSuppliers: ToolDefinition<typeof args> = {
  name: "suppliers-update-suppliers",
  description: `Update a Supplier


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_suppliers**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await suppliersUpdateSuppliers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
