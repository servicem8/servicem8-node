/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { taxRatesDeleteTaxRates } from "../../funcs/taxRatesDeleteTaxRates.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteTaxRatesRequest$inboundSchema,
};

export const tool$taxRatesDeleteTaxRates: ToolDefinition<typeof args> = {
  name: "tax-rates-delete-tax-rates",
  description: `Delete a Tax Rate


			
In ServiceM8, deleting a record sets its \`active\` field to \`0\`. Inactive records are still accessible on the API, but are hidden in the UI. Inactive records can be restored by setting their \`active\` field to \`1\`.

			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_tax_rates**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await taxRatesDeleteTaxRates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
