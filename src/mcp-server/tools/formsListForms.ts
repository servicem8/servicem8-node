/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { formsListForms } from "../../funcs/formsListForms.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$formsListForms: ToolDefinition = {
  name: "forms-list-forms",
  description: `List all Forms


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_forms**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await formsListForms(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
