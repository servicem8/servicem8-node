/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxAttachInboxMessageToJob } from "../../funcs/inboxAttachInboxMessageToJob.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.AttachInboxMessageToJobRequest$inboundSchema,
};

export const tool$inboxAttachInboxMessageToJob: ToolDefinition<typeof args> = {
  name: "inbox-attach-inbox-message-to-job",
  description: `Attach message to existing job

Attaches an inbox message to an existing job`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxAttachInboxMessageToJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
