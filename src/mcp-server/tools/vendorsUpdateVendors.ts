/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { vendorsUpdateVendors } from "../../funcs/vendorsUpdateVendors.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateVendorsRequest$inboundSchema,
};

export const tool$vendorsUpdateVendors: ToolDefinition<typeof args> = {
  name: "vendors-update-vendors",
  description: `Update a Vendor

Vendor account information

`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await vendorsUpdateVendors(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
