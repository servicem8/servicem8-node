/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxCreateInboxMessage } from "../../funcs/inboxCreateInboxMessage.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.CreateInboxMessageRequest$inboundSchema,
};

export const tool$inboxCreateInboxMessage: ToolDefinition<typeof args> = {
  name: "inbox-create-inbox-message",
  description: `Create a new inbox message

Creates a new inbox message that will appear in the inbox`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxCreateInboxMessage(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
