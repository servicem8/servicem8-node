/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { allocationWindowsUpdateAllocationWindows } from "../../funcs/allocationWindowsUpdateAllocationWindows.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateAllocationWindowsRequest$inboundSchema,
};

export const tool$allocationWindowsUpdateAllocationWindows: ToolDefinition<
  typeof args
> = {
  name: "allocation-windows-update-allocation-windows",
  description: `Update an Allocation Window`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await allocationWindowsUpdateAllocationWindows(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
