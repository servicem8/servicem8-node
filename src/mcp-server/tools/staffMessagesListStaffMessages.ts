/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { staffMessagesListStaffMessages } from "../../funcs/staffMessagesListStaffMessages.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$staffMessagesListStaffMessages: ToolDefinition = {
  name: "staff-messages-list-staff-messages",
  description: `List all Staff Messages


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_messages**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await staffMessagesListStaffMessages(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
