/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentTemplatesUpdateDocumentTemplates } from "../../funcs/documentTemplatesUpdateDocumentTemplates.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateDocumentTemplatesRequest$inboundSchema,
};

export const tool$documentTemplatesUpdateDocumentTemplates: ToolDefinition<
  typeof args
> = {
  name: "document-templates-update-document-templates",
  description: `Update a Document Template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentTemplatesUpdateDocumentTemplates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
