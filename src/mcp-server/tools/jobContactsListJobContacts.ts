/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobContactsListJobContacts } from "../../funcs/jobContactsListJobContacts.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$jobContactsListJobContacts: ToolDefinition = {
  name: "job-contacts-list-job-contacts",
  description: `List all Job Contacts


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_job_contacts**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await jobContactsListJobContacts(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
