/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { clientsUpdateClients } from "../../funcs/clientsUpdateClients.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateClientsRequest$inboundSchema,
};

export const tool$clientsUpdateClients: ToolDefinition<typeof args> = {
  name: "clients-update-clients",
  description: `Update a Client


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_customers**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await clientsUpdateClients(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
