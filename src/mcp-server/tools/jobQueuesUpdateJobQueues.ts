/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobQueuesUpdateJobQueues } from "../../funcs/jobQueuesUpdateJobQueues.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateJobQueuesRequest$inboundSchema,
};

export const tool$jobQueuesUpdateJobQueues: ToolDefinition<typeof args> = {
  name: "job-queues-update-job-queues",
  description: `Update a Job Queue


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_job_queues**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jobQueuesUpdateJobQueues(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
