/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxConvertInboxMessageToJob } from "../../funcs/inboxConvertInboxMessageToJob.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ConvertInboxMessageToJobRequest$inboundSchema,
};

export const tool$inboxConvertInboxMessageToJob: ToolDefinition<typeof args> = {
  name: "inbox-convert-inbox-message-to-job",
  description: `Convert message to job

Converts an inbox message into a new job, optionally using a job template`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxConvertInboxMessageToJob(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
