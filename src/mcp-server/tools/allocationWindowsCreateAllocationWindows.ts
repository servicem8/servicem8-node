/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { allocationWindowsCreateAllocationWindows } from "../../funcs/allocationWindowsCreateAllocationWindows.js";
import * as components from "../../models/components/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: components.AllocationWindowInput$inboundSchema,
};

export const tool$allocationWindowsCreateAllocationWindows: ToolDefinition<
  typeof args
> = {
  name: "allocation-windows-create-allocation-windows",
  description: `Create a new Allocation Window


			
#### Record UUID
UUID is optional for record creation. If no UUID is supplied, a UUID will be automatically generated for the new record and returned in the response header as x-record-uuid.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await allocationWindowsCreateAllocationWindows(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value.result;

    return formatResult(value, apiCall);
  },
};
