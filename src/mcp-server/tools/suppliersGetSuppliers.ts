/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { suppliersGetSuppliers } from "../../funcs/suppliersGetSuppliers.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetSuppliersRequest$inboundSchema,
};

export const tool$suppliersGetSuppliers: ToolDefinition<typeof args> = {
  name: "suppliers-get-suppliers",
  description: `Retrieve a Supplier


			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_suppliers**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await suppliersGetSuppliers(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
