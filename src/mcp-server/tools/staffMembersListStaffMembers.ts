/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { staffMembersListStaffMembers } from "../../funcs/staffMembersListStaffMembers.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$staffMembersListStaffMembers: ToolDefinition = {
  name: "staff-members-list-staff-members",
  description: `List all Staff Members


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_staff**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await staffMembersListStaffMembers(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
