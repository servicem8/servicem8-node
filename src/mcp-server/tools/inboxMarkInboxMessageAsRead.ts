/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { inboxMarkInboxMessageAsRead } from "../../funcs/inboxMarkInboxMessageAsRead.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.MarkInboxMessageAsReadRequest$inboundSchema,
};

export const tool$inboxMarkInboxMessageAsRead: ToolDefinition<typeof args> = {
  name: "inbox-mark-inbox-message-as-read",
  description: `Mark message as read

Marks an inbox message as read`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await inboxMarkInboxMessageAsRead(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
