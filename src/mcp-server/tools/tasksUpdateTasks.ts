/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { tasksUpdateTasks } from "../../funcs/tasksUpdateTasks.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateTasksRequest$inboundSchema,
};

export const tool$tasksUpdateTasks: ToolDefinition<typeof args> = {
  name: "tasks-update-tasks",
  description: `Update a Task


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_tasks**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await tasksUpdateTasks(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
