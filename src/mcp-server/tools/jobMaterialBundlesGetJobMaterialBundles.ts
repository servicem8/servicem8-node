/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobMaterialBundlesGetJobMaterialBundles } from "../../funcs/jobMaterialBundlesGetJobMaterialBundles.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetJobMaterialBundlesRequest$inboundSchema,
};

export const tool$jobMaterialBundlesGetJobMaterialBundles: ToolDefinition<
  typeof args
> = {
  name: "job-material-bundles-get-job-material-bundles",
  description: `Retrieve a Job Material Bundle


			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_job_materials**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await jobMaterialBundlesGetJobMaterialBundles(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
