/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobMaterialsUpdateJobMaterials } from "../../funcs/jobMaterialsUpdateJobMaterials.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateJobMaterialsRequest$inboundSchema,
};

export const tool$jobMaterialsUpdateJobMaterials: ToolDefinition<typeof args> =
  {
    name: "job-materials-update-job-materials",
    description: `Update a Job Material


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_job_materials**.

			`,
    args,
    tool: async (client, args, ctx) => {
      const [result, apiCall] = await jobMaterialsUpdateJobMaterials(
        client,
        args.request,
        { fetchOptions: { signal: ctx.signal } },
      ).$inspect();

      if (!result.ok) {
        return {
          content: [{ type: "text", text: result.error.message }],
          isError: true,
        };
      }

      const value = result.value;

      return formatResult(value, apiCall);
    },
  };
