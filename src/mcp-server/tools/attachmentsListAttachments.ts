/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attachmentsListAttachments } from "../../funcs/attachmentsListAttachments.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$attachmentsListAttachments: ToolDefinition = {
  name: "attachments-list-attachments",
  description: `List all Attachments


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_attachments**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await attachmentsListAttachments(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
