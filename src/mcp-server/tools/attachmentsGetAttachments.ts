/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { attachmentsGetAttachments } from "../../funcs/attachmentsGetAttachments.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.GetAttachmentsRequest$inboundSchema,
};

export const tool$attachmentsGetAttachments: ToolDefinition<typeof args> = {
  name: "attachments-get-attachments",
  description: `Retrieve an Attachment


			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_attachments**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await attachmentsGetAttachments(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
