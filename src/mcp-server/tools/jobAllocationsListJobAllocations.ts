/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { jobAllocationsListJobAllocations } from "../../funcs/jobAllocationsListJobAllocations.js";
import { formatResult, ToolDefinition } from "../tools.js";

export const tool$jobAllocationsListJobAllocations: ToolDefinition = {
  name: "job-allocations-list-job-allocations",
  description: `List all Job Allocations


			
#### Filtering
This endpoint supports result filtering. For more information on how to filter this request, [go here](/docs/filtering).
			
			
#### OAuth Scope
This endpoint requires the following OAuth scope **read_schedule**.

			`,
  tool: async (client, ctx) => {
    const [result, apiCall] = await jobAllocationsListJobAllocations(
      client,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
