/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { searchObjectSearch } from "../../funcs/searchObjectSearch.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.ObjectSearchRequest$inboundSchema,
};

export const tool$searchObjectSearch: ToolDefinition<typeof args> = {
  name: "search-object-search",
  description: `Search within a specific object type

Performs a text search within a specific object type. Supported types: job, company, material, knowledgearticle, attachment, formresponse, asset, materialbundle`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await searchObjectSearch(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
