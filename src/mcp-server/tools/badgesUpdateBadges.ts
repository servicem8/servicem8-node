/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { badgesUpdateBadges } from "../../funcs/badgesUpdateBadges.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.UpdateBadgesRequest$inboundSchema,
};

export const tool$badgesUpdateBadges: ToolDefinition<typeof args> = {
  name: "badges-update-badges",
  description: `Update a Badge


			
#### OAuth Scope
This endpoint requires the following OAuth scope **manage_badges**.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await badgesUpdateBadges(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
