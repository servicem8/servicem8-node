/*
 * Code generated by Speakeasy (https://speakeasy.com). DO NOT EDIT.
 */

import { documentTemplatesDeleteDocumentTemplates } from "../../funcs/documentTemplatesDeleteDocumentTemplates.js";
import * as operations from "../../models/operations/index.js";
import { formatResult, ToolDefinition } from "../tools.js";

const args = {
  request: operations.DeleteDocumentTemplatesRequest$inboundSchema,
};

export const tool$documentTemplatesDeleteDocumentTemplates: ToolDefinition<
  typeof args
> = {
  name: "document-templates-delete-document-templates",
  description: `Delete a Document Template


			
In ServiceM8, deleting a record sets its \`active\` field to \`0\`. Inactive records are still accessible on the API, but are hidden in the UI. Inactive records can be restored by setting their \`active\` field to \`1\`.

			`,
  args,
  tool: async (client, args, ctx) => {
    const [result, apiCall] = await documentTemplatesDeleteDocumentTemplates(
      client,
      args.request,
      { fetchOptions: { signal: ctx.signal } },
    ).$inspect();

    if (!result.ok) {
      return {
        content: [{ type: "text", text: result.error.message }],
        isError: true,
      };
    }

    const value = result.value;

    return formatResult(value, apiCall);
  },
};
